@model IEnumerable<E_Learning.Models.StuSubTea>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ReDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StudentSubject.StuSubID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Teacher.TeachName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ReDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StudentSubject.StuSubID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Teacher.TeachName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StuSubTeaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.StuSubTeaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StuSubTeaID })
        </td>
    </tr>
}

</table>
